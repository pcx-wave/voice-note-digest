# Core
python-telegram-bot>=20.0
requests>=2.28
PyYAML>=6.0

# Optional: local speaker identification (resemblyzer).
# Heavy on a Raspberry Pi — install only if you want per-speaker attribution.
#   pip install resemblyzer numpy
# ffmpeg must also be available on PATH for audio conversion.
